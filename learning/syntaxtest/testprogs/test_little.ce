def main(): int {
    var i: int;

    write "Digite os valores do arranjo:\n";

    for (i=0; i<10; i+=1) {
        write "A[", i, "] = ";
        read v[i];
    }

    bubblesort(v, 10);

    write "Arranjo ordenado:\nA = ";

    for (i=0; i<10; i+=1) {
        write v[i], " ";
    }

}
