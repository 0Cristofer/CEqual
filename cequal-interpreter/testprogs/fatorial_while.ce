def main(): int{
    var num, n : int;

    def fatorial(): int{
        write "Fatorial de: ";
        read num;
  
        n = num - 1;

        while(n > 1){
            num = num * n;
            n -= 1;
        }
        return num;
    }

    //fatorial();
    //write num;
    print(fatorial());
    return 0;
}

def print(a : int){
    write a;
}