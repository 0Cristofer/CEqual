def add(a,b: int) : int{
  return a + b;
}

def main() : int {
  var x,y : int;
  read x;
  read y;

  if(x > y){
    write "x maior que y";
  }
  else
        if(x == y) write "x igual a y";
        else write "x menor que y";

   write "meu cu";
  return 0;
}
