def xxx(x[]: int; y: string){
  read y;
  x[1] += 2;
  write y;
}

def main(): int{
  var a[3] = {22, 59, 101}, b = 4: int;
  var tt = true: bool;
  var s: string;

  read s;
  write s;

  a[1] -= b;

  xxx(a, s);
  write s;

  write a[1];
}
