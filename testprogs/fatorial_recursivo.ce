def fat(n : int) : int {
    if(n == 0) return 0;
    if(n == 1) return 1;

    return n * fat(n-1);
}

def main() : int {
    var n : int;

    write "Fatorial de: ";
    read n;

    write fat(n);
    
    return 0;
}
